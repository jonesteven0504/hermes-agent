# 整体框架

## 工具及工具调用

### file_tools.py 的关键体会

1. tool schema 不只是函数声明，也是给 LLM 的行为提示词。
   - 例如 `read_file` 明确说“用它替代 cat/head/tail”，是在引导模型走有护栏的路径，而不是随意调用 shell。
   - schema 中的 `offset`、`limit`、输出格式、失败提示，会影响模型如何规划工具调用。

2. “低风险”是指把模型引导到可控工具，而不是高自由度 shell。
   - 文件工具内部有设备文件阻断、二进制文件阻断、敏感路径保护、secret redaction。
   - 所以 schema 让模型优先调用 file tool，本质是在降低不可控 IO 和 prompt injection 风险。

3. “低 token”是指让模型按需读取，而不是一次性吞大文件。
   - `read_file` 默认分页，支持 `offset + limit`。
   - 对过大读取结果会拒绝，并提示缩小范围。
   - 重复读取同一文件区域时，会用 dedup 返回轻量提示，避免重复塞上下文。

4. “可恢复”是指失败后模型知道下一步怎么做。
   - 搜索结果截断时提示下一个 `offset`。
   - patch 找不到 `old_string` 时提示重新 `read_file` 或 `search_files`。
   - patch schema 要求 `old_string` 唯一并包含上下文，是为了让编辑更容易成功和校验。

5. `task_id` 是工具执行状态边界，不等于聊天会话边界。
   - 默认每次 `run_conversation` 会生成一个新的 `task_id`。
   - 同一 `task_id` 下共享文件操作环境、读文件记忆、重复读取计数、mtime 记录。
   - `session_id` 更偏对话历史；`task_id` 更偏本轮工具执行。

6. `_read_tracker` 是 LLM 的外置短期操作记忆。
   - `last_key` / `consecutive`：发现模型是否反复读/搜同一内容。
   - `dedup`：同一区域未变化时不重复返回原文。
   - `read_timestamps`：写入前判断文件是否在读取后被外部修改。

7. 状态管理的核心价值：
   - 防止上下文爆炸。
   - 防止模型陷入重复工具调用循环。
   - 防止基于旧文件内容写入。
   - 保证 terminal/file 工具在同一任务中看到一致的执行环境。

---

## System Prompt 构建机制（2025-04-29 学习笔记）

### 1. 五层架构
System Prompt 不是单一文本，而是**五层动态组装**的结果：

| 层级 | 来源 | 内容示例 | 代码位置 |
|------|------|----------|----------|
| 身份层 | prompt_builder.py | "You are Hermes, an AI Agent..." | `_build_system_prompt()` 开头 |
| 平台层 | PLATFORM_HINTS | CLI: "尽量少用 markdown" | `agent/prompt_builder.py:291` |
| 工具层 | TOOL_USE_PREFIX | 工具调用格式规范 | `agent/prompt_builder.py` |
| 技能层 | _build_skill_index() | 可用 skills 列表 | 动态扫描 `~/.hermes/skills/` |
| 记忆层 | Memory Manager | 长期记忆/短期记忆 | `agent/memory_manager.py` |

### 2. 缓存策略（关键优化）
- **每会话只构建一次**：`_cached_system_prompt` 变量缓存
- **重建触发条件**：仅上下文压缩后（`context_compressor.compress()`）
- **跨实例续接**：Gateway 从 SQLite `session.system_prompt` 加载旧值
- **Anthropic 优化**：保持 system prompt **字节级不变**以利用 prefix caching 降低成本

### 3. 关键代码锚点
```python
# 构建入口
run_agent.py:3929  _build_system_prompt()

# 缓存使用
run_agent.py:7288  api_messages = [{"role": "system", "content": self._cached_system_prompt}] + ...

# 平台差异注入
run_agent.py:4090-4092  platform_key in PLATFORM_HINTS

# 缓存失效（压缩后重建）
run_agent.py:1976  self._cached_system_prompt = None
```

---

## Context Compression（上下文压缩）

### 1. 触发条件
- messages 总 tokens > `threshold_tokens`（通常是 context_length 的 50-70%）
- 由 `context_compressor.compress_messages()` 处理

### 2. 压缩策略
- 早期消息摘要化（保留关键信息）
- 大工具结果 truncation
- 保留决策点（用户指令、关键工具调用）

### 3. 与 System Prompt 的关系
```
上下文太长 → 触发压缩 → 重建 system_prompt（因为记忆变了）
                                    ↓
                        Anthropic cache 失效（内容变了）
                                    ↓
                        下次 API 请求费用增加
```

---

## Self-Reflection（自我反思 / Skill 自校正）

### 1. 触发机制
```python
run_agent.py:11728-11732
if (_iters_since_skill >= _skill_nudge_interval  # 默认每 10 轮工具调用
    and "skill_manage" in self.valid_tool_names):
    _should_review_skills = True
```

### 2. 后台异步执行
```python
run_agent.py:11746-11754
_spawn_background_review(
    messages_snapshot=list(messages),
    review_skills=True
)
```

### 3. 核心 Prompt（Shadow Agent 使用）
```python
_SKILL_REVIEW_PROMPT = (
    "Review the conversation above and consider saving or updating a skill..."
    "Focus on: was a non-trivial approach used..."
)
```

### 4. Skill 操作工具
- `_create_skill(name, content, category)` → 新建 `~/.hermes/skills/<category>/<name>/SKILL.md`
- `_patch_skill(name, old_str, new_str)` → 修改已有 skill

### 5. 自我改进闭环
```
对话经验 → 触发反思 → 创建/更新 skill → 下次 system prompt 包含新 skill → 
LLM 知道新技能 → 未来对话使用 → 更多经验...
```

---

## Session 持久化与 Gateway 架构

### 1. Session 存储
- **位置**：`~/.hermes/state.db` (SQLite)
- **内容**：messages、system_prompt、metadata
- **有效期**：理论上永久（直到用户 `/new` 或删文件）

### 2. Gateway 无状态设计
```
Alice 消息 A ──→ 创建 Agent A ──→ 运行 ──→ 保存到 DB ──→ 销毁 Agent A
Alice 消息 B ──→ 创建 Agent B ──→ 加载历史 ──→ 运行 ──→ 保存 ──→ 销毁 Agent B
         ↑
    每次新建 Python 对象（避免并发冲突）
    但 session_id 相同 → 从 DB 加载相同上下文
```

### 3. 为什么不能复用 Agent 实例
- **并发冲突**：同一实例被多个请求同时修改 `self.messages`
- **内存泄露**：长期运行积累状态不释放
- **容错性**：崩溃只影响当前请求

### 4. Session ID 生成规则
- Telegram: `telegram_{chat_id}_{user_id}`
- Discord: `discord_{guild_id}_{channel_id}_{user_id}`
- CLI: 每次启动新 UUID（除非 `/resume`）

---

## 工具注册与运行时门禁

### 1. 三层过滤模型
```
所有 registry.register() 的工具 (~50+)
        ↓ 过滤层 1: toolset 选择（如 hermes-cli）
工具集内的工具 (~20)
        ↓ 过滤层 2: check_fn 运行时检测
LLM 实际看到的工具 (~10-15)
```

### 2. web_search 示例
```python
tools/web_tools.py:2080-2089
registry.register(
    name="web_search",
    toolset="web",
    check_fn=check_web_api_key,  # ← 运行时门禁
    requires_env=["TAVILY_API_KEY", "EXA_API_KEY", ...]
)
```

### 3. 设计哲学
- 没 key 的工具**对 LLM 不可见**（避免调用后崩溃）
- check_fn 失败 → 工具从 `self.tools` 剔除 → API 请求不包含

---

## 关键认知总结

1. **System Prompt 是动态组装的**（不是静态文件），5 层叠加，~7-10K tokens
2. **Prompt Caching 是商业优化**（Anthropic 按字节匹配收费），Hermes 拼命保持稳定
3. **Session 是磁盘持久化的**（SQLite），不是内存临时数据
4. **Agent 实例是临时的**，Session 数据是永久的（无状态架构）
5. **工具可见性 ≠ 工具存在性**：注册了的工具可能被运行时过滤掉
6. **自我反思是异步后台的**，不影响主对话响应时间
7. **3B 小模型能验证机制存在，但不能产出高质量 skill**（需要 70B+ 或云模型）
