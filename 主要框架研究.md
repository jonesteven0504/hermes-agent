# Hermes Agent 主框架研究计划（1-2 天）

## 研究目标

本轮研究只围绕领导要求的三条主线展开：

1. **主 Agent 框架**：研究主流程、调用链路，以及 memory、skills 如何被加入整体流程；重点理解意图识别、任务拆解、工具调用和最终回复是如何在主循环中发生的。
2. **Memory 机制**：研究 memory 如何存、如何取、如何进入 prompt，以及长上下文时如何被压缩或重建。
3. **Skills 机制**：研究 skill 如何定义、如何被发现、如何注入 prompt，以及如何由自我反思机制新建和更新。

当前优先级：**先研究主 Agent 框架，Memory 和 Skills 只在它们接入主流程的位置展开，不先深入旁支实现。**

---

## 防走偏边界

本阶段暂时不研究：

- `gateway/` 多平台适配细节，除非用于理解 session 续接。
- `tools/file_tools.py` 内部状态管理细节，除非用于理解工具结果如何回到 messages。
- `agent/auxiliary_client.py` 的 provider 路由细节。
- 本地模型部署、llama.cpp 参数、OpenRouter/API key 配置。
- 单个工具的复杂实现，例如 web、browser、mcp。

判断是否走偏的标准：

> 如果一个问题不能帮助回答“主 Agent 如何从用户输入走到回复、memory/skills 如何进入这条链路”，就先记录为后续问题，不在本轮深入。

---

## Day 1：主 Agent 框架调用链路

### 目标

画出 `AIAgent` 从创建、接收用户输入、组装 prompt、调用模型、执行工具、回写 messages、生成最终回复的完整链路。

### 关键切入点

| 研究问题 | 重点文件 | 需要定位的源码点 |
|---|---|---|
| `AIAgent` 如何创建？ | `run_agent.py`, `cli.py` | `class AIAgent`, `__init__`, `AIAgent(...)` 调用点 |
| 用户输入进入哪里？ | `run_agent.py` | `chat()`, `run_conversation()` |
| system prompt 如何组装？ | `run_agent.py`, `agent/prompt_builder.py` | `_build_system_prompt()`, `PLATFORM_HINTS`, `_cached_system_prompt` |
| tools 如何给到模型？ | `run_agent.py`, `model_tools.py`, `toolsets.py` | `get_tool_definitions()`, `self.tools`, `api_kwargs["tools"]` |
| 模型回复如何分叉？ | `run_agent.py` | 判断 `tool_calls` vs 普通文本回复 |
| 工具如何执行并回流？ | `run_agent.py`, `model_tools.py` | `_execute_tool_calls()`, `handle_function_call()`, `messages.append(tool_msg)` |
| 最终回复如何返回？ | `run_agent.py` | `final_response`, result dict |

### 研究步骤

1. **定位 Agent 生命周期入口**
   - 找 `class AIAgent`
   - 找 `__init__`
   - 找 `chat()` 和 `run_conversation()`
   - 输出：写下“用户调用入口”和“主循环入口”的区别。

2. **定位主循环**
   - 在 `run_conversation()` 中找到 `while` 循环。
   - 标出循环终止条件：最大 API 调用次数、iteration budget、final response、interrupt/error。
   - 输出：画出一轮循环的最小结构。

3. **定位 prompt 进入 messages 的方式**
   - 找 `_build_system_prompt()`
   - 找 `_cached_system_prompt`
   - 找 system prompt 被加入 API messages 的位置。
   - 输出：说明 system prompt、user message、history messages 的关系。

4. **定位工具调用链路**
   - 找 `self.tools = get_tool_definitions(...)`
   - 找 `api_kwargs["tools"] = self.tools`
   - 找模型返回 `tool_calls` 后的执行函数。
   - 输出：画出 `tool schema -> LLM -> tool_calls -> handler -> tool result -> messages` 链路。

5. **定位“意图识别、任务拆解”的真实位置**
   - 重点结论：Hermes 里大部分意图识别和任务拆解不是传统规则引擎，而是由 LLM 在 system prompt、tools schema、skills index 的约束下生成下一步动作。
   - 需要验证：
     - 是否存在显式 intent classifier。
     - 是否主要依赖 LLM 根据 messages 和 tools 决策。
   - 输出：用代码证据说明“意图识别/任务拆解”发生在模型生成 `tool_calls` 或直接回复这一刻。

### Day 1 产物

- 一张主 Agent 调用链路图。
- 一张 messages 增长表：
  - `system`
  - `user`
  - `assistant(tool_calls)`
  - `tool`
  - `assistant(final)`
- 一段文字回答：
  - “Hermes 的主 Agent 是如何把用户输入转为工具调用或最终回复的？”

---

## Day 2 上午：Memory 如何接入主流程

### 目标

理解 memory 不是独立聊天历史，而是会被加载、格式化并注入 system prompt 或压缩流程的持久上下文。

### 关键切入点

| 研究问题 | 重点文件 | 需要定位的源码点 |
|---|---|---|
| memory 管理类在哪里？ | `agent/memory_manager.py` | `MemoryManager`, `build_system_prompt()` |
| memory 从哪里读？ | `agent/memory_manager.py`, `hermes_state.py` | disk/db load 相关函数 |
| memory 如何进入 prompt？ | `run_agent.py`, `agent/memory_manager.py` | `_memory_manager.build_system_prompt()` |
| memory 如何写入？ | `run_agent.py`, memory 工具相关文件 | memory tool / background review |
| 压缩后为何重建 prompt？ | `run_agent.py`, `agent/context_compressor.py` | `_invalidate_system_prompt()`, context compression |

### 研究步骤

1. 从 `run_agent.py` 中搜索 `_memory_manager`，找到初始化、读取和 prompt 注入位置。
2. 打开 `agent/memory_manager.py`，只看 `build_system_prompt()` 及其直接依赖。
3. 找 memory 存储路径，不深入 SQLite schema，只确认“存在哪里、何时读”。
4. 找压缩触发后为何 `self._cached_system_prompt = None`，理解 memory 变化如何导致 prompt 重建。

### Day 2 上午产物

- 一句话定义 memory：
  - “Memory 是如何从持久存储进入 system prompt 的？”
- 一张小图：
  - `memory store -> MemoryManager -> system prompt -> model`

---

## Day 2 下午：Skills 如何定义、生成、更新

### 目标

理解 skills 是给 LLM 的“操作手册”，不是可执行工具；它们通过 skill index 进入 prompt，并可被 self-reflection 后台机制创建或更新。

### 关键切入点

| 研究问题 | 重点文件 | 需要定位的源码点 |
|---|---|---|
| skill 文件格式是什么？ | `skills/**/SKILL.md` | YAML frontmatter + Markdown instructions |
| skills 如何被发现？ | `agent/prompt_builder.py`, `agent/skill_utils.py` | skill index 扫描函数 |
| skills 如何进入 system prompt？ | `agent/prompt_builder.py` | `_build_skill_index()` 相关逻辑 |
| 新 skill 如何创建？ | `tools/skill_manager_tool.py` | `_create_skill()` |
| 旧 skill 如何更新？ | `tools/skill_manager_tool.py` | `_patch_skill()` |
| 自我反思如何触发？ | `run_agent.py` | `_skill_nudge_interval`, `_spawn_background_review`, `_SKILL_REVIEW_PROMPT` |

### 研究步骤

1. 先看一个已有 skill：例如 `skills/software-development/systematic-debugging/SKILL.md`。
2. 找 `prompt_builder.py` 中 skill index 的构建逻辑，只关注“如何把 skill 摘要放进 prompt”。
3. 找 `run_agent.py` 中 self-reflection 触发条件：
   - `_skill_nudge_interval`
   - `_iters_since_skill`
   - `skill_manage in self.valid_tool_names`
4. 找 `tools/skill_manager_tool.py` 中 `_create_skill()` 和 `_patch_skill()`，确认 skill 文件如何落盘。

### Day 2 下午产物

- 一句话区分 tools 和 skills：
  - Tool 是程序能力，Skill 是写给 LLM 的经验说明书。
- 一张闭环图：
  - `conversation -> background review -> skill_manage -> SKILL.md -> next system prompt`

---

## 最终交付物

完成 1-2 天研究后，需要能回答领导的三个问题：

1. **主 Agent 框架如何调用？**
   - 从入口、prompt、messages、API call、tool_calls、tool result、final response 说明完整链路。

2. **Memory 如何存、取、压缩？**
   - 说明 memory 的持久化位置、读取入口、注入 prompt 的位置，以及压缩后为何需要重建 prompt。

3. **Skills 如何定义、生成、新建、更新？**
   - 说明 skill 文件格式、skill index 注入、self-reflection 触发、新建与 patch 的工具函数。

---

## 当前下一步

先从 **Day 1 / 步骤 1：定位 Agent 生命周期入口** 开始。

建议依次定位：

```bash
grep -n "class AIAgent" run_agent.py
grep -n "def __init__" run_agent.py | head -5
grep -n "def chat" run_agent.py
grep -n "def run_conversation" run_agent.py
```

只记录行号和作用，不阅读大段实现。



# ************************* 研究成果 ************************ 

# agent的研究
## agent的创建



## 用户的输入是如何接收的

## prompt的拼接

## 意图是如何识别的？工具是如何调用的？


## 工具是如何具体执行的？


## 模型是如何回复的？

## message信息是如何回写的？





# memeory的研究


# skills的研究





# ----------------主要的代码框架-------------

类：
_safewriter（负责安全输出，避免非法字符）
Iterationbudge（管理agent每轮可用的迭代预算，避免无限循环）
AIagent（负责agent的创建和初始化，调用并执行工具，压缩上下文等）


关键的函数：
外部的函数： 大多都是纯工具函数，如读取环境变量中代理配置、判断shell命令、判断调用涉及的文件路径是否重叠等（防止并行操作），修复模型生成的不规范 tool call 参数等；


## AIAgent 的核心不是某个工具实现，而是把 prompt、messages、tools、memory、skills、API client、session 串成一个可运行主循环。